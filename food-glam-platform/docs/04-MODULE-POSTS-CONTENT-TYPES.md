# Module: Posts (Content Types)## Types-recipe -short (external link) -image -video (FULL video external link) ## DB`posts`-id (uuid) -type (recipe|short|image|video) -title -slug (unique) -approach_id -created_by -created_at -status (active|pending|rejected) -hero_image_url (optional) -video_url (for short/video/optional recipe) -caption (short/image/video) -recipe_json (jsonb only for recipes) ## Routes-`/recipes`, `/shorts`, `/videos` list pages -`/recipes/[slug]`-Optionally `/p/[slug]` generic post detail for non-recipes ## Acceptance criteria-Can browse each content type and open its viewer.